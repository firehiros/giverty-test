export enum FIELD_TYPE {
  TEXT = 'text',
  RICH_TEXT = 'richtext',
  NUMBER = 'number',
  DATE = 'date',
  BOOLEAN = 'boolean',
  EMAIL = 'email',
  PASSWORD = 'password',
  UID = 'uid',
  MEDIA = 'media',
  PHOTO = 'photo',
  JSON = 'json',
  ENUM = 'enum',
}
